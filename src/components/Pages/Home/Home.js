import React from 'react';
import { Spinner } from '../../';
import * as S from './Home.styles';

const Home = () => {
  return (
    <S.ContainerStyle>
      <Spinner />
    </S.ContainerStyle>
  );
};

export default Home;
